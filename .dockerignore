# Build artefacts produced by colcon
build/
install/
log/

# Frontend deps. Copied into the builder by `COPY src/` otherwise, which on a
# `hexa deploy build` means dragging them through qemu.
**/node_modules

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/

# VCS / editor / OS
.git/
.gitignore
.vscode/
.idea/
*.swp
.DS_Store

# Local plan / scratch
docs/plans/
